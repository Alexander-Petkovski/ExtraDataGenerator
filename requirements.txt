# ExtraDataGenerator — runtime dependencies
# Install with:  pip install -r requirements.txt

pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0       # Excel write (.xlsx)

# ── to build the .exe (Windows) ─────────────────────────────────────────────
# Just run  build_exe.bat  — it installs everything automatically.
# Or manually:  pip install pyinstaller && pyinstaller ExtraDataGenerator.spec
